<UserControl x:Class="WpfApp1.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="225">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="3" VerticalAlignment="Center">
            <Label FontSize="16" Content="{Binding Nickname}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="3" VerticalAlignment="Center">
            <Label FontSize="16" Content="{Binding Phone}"/>
        </StackPanel>
    </Grid>
</UserControl>
